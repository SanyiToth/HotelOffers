import { ParenthesesPipe } from './parentheses.pipe';

describe('ParenthesesPipe', () => {
  it('create an instance', () => {
    const pipe = new ParenthesesPipe();
    expect(pipe).toBeTruthy();
  });
});
